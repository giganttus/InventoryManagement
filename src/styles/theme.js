import { createTheme } from "@mui/material/styles";

const theme = {
	palette: {
		primary: {
			main: "#6532CF",
		},
		secondary: {
			main: "#ffffff",
		},
	},
	shape: {
		borderRadius: 8,
	},
};

export default createTheme(theme);
